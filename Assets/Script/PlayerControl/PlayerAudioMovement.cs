using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAudioMovement : MonoBehaviour
{
    //Obs Não precisava desse script para controlar o audio, mas
    //decidi cria-lo para ter diferentes volumes no mesmo objeto

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
